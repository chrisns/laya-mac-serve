# The Python packages that ship inside LayaServe.app.
# Versions are pinned so that every build is the same.
fastapi==0.141.1
uvicorn==0.53.0
laya==0.3.4
torch==2.14.0
transformers==5.17.0
huggingface-hub==1.32.0
safetensors==0.8.0
tokenizers==0.23.2
numpy==2.5.3
